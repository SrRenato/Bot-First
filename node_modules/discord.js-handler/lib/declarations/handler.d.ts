import { Client } from 'discord.js';
import BasePropsObject from '../structures/BasePropsObject';
export declare class HandlerBase extends BasePropsObject {
    protected readonly moduleFolder: string;
    protected readonly client: Client;
    protected readonly token?: string;
    protected readonly verbose?: boolean;
    protected readonly eventsFolder: string;
    protected readonly commandsFolder?: string;
    protected readonly finalizersFolder?: string;
}
export declare type HandlerOptions = Omit<HandlerBase, 'moduleFolder' | 'addProps' | 'props'>;
export interface PropsType {
    [key: string]: unknown;
}
