import { Collection } from 'discord.js';
import { GenericCommand, CommandCache, CommandProperties, AnyGenericCommandType } from '../declarations/commands';
export default class CommandsCache {
    static readonly commands: CommandCache<'listener'>;
    static readonly properties: CommandProperties;
    static readonly categories: Collection<string, GenericCommand['aliases']>;
    static readonly findSelfProperty: <T extends AnyGenericCommandType>(collection: CommandCache<T>, commandName: string) => GenericCommand[T];
}
